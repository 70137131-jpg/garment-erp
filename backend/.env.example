# Copy to .env and adjust. SQLite is the zero-setup default for local dev.
# For production use Postgres:
# DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/garment_erp
DATABASE_URL=sqlite:///./garment_erp.db
